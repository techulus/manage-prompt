version: 2

project_name: manageprompt

builds:
  - main: ./cmd/manageprompt
    binary: manageprompt
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}

archives:
  - formats: ['tar.gz']
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats:
          - zip

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

brews:
  - repository:
      owner: techulus
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Formula
    homepage: https://github.com/techulus/manage-prompt
    description: Local LLM call debugger
    license: MIT
    install: |
      bin.install "manageprompt"
    test: |
      system "#{bin}/manageprompt", "version"

release:
  github:
    owner: techulus
    name: manage-prompt
