TOKEN "workflow_runs_count_by_hour_endpoint_read_7341" READ

NODE workflow_runs_count_by_hour_0
SQL >

    SELECT HOUR(timestamp) as hour, workflow_id, count(*) as total FROM run_workflow GROUP BY HOUR(timestamp), workflow_id



NODE workflow_runs_count_by_hour_1
SQL >

    %
    SELECT hour, total FROM workflow_runs_count_by_hour_0 where workflow_id = {{Int16(workflow_id, required=True)}} limit 24


